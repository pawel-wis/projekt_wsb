<h1>Product</h1>

{{#if hasProducts }}
<ul>
    {{#each products }}
    <ul>
        <li>{{ this.name }}</li>
        <li>{{ this.price }}</li>
        <li>{{ this.description }}</li>
    </ul>

    {{/each}}
</ul>
{{ else }}
<h1>No products found !</h1>
{{/if}}